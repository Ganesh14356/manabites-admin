[build]
  command   = "npm run build"
  publish   = "dist"
  functions = "netlify/functions"

# SPA redirect — send all routes to index.html so React Router works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
